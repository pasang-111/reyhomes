Django>=5.0,<6.0
djangorestframework>=3.15
django-cors-headers>=4.3
Pillow>=10.0
psycopg2-binary>=2.9
python-dotenv>=1.0
django-filter>=24.0
gunicorn>=22.0
whitenoise>=6.6
django-unfold>=0.104

djangorestframework-simplejwt>=5.3,<6.0

# Optional but recommended for production media storage (S3-compatible).
# See DEPLOYMENT_GUIDE.md — required once you move off local disk storage.
django-storages[s3]>=1.14
boto3>=1.34

# Combined floorplan + inclusions PDF generation
reportlab>=4.2

# Cloudinary media (free tier) — set USE_CLOUDINARY=True on Render
cloudinary>=1.40
django-cloudinary-storage>=0.3.0

